query GetBlogPosts {
  blogPosts(stage: DRAFT, orderBy: publishedDate_DESC, first: 20) {
    id
    slug
    title
    category
    publishedDate
    readTime
    summary
    imageUrl
  }
}

query GetBlogPost($slug: String!) {
  blogPost(where: { slug: $slug }, stage: DRAFT) {
    id
    slug
    title
    category
    publishedDate
    readTime
    summary
    imageUrl
    body {
      html
    }
  }
}
