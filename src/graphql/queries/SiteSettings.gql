query GetSiteSettings($locale: Locale!) {
  allSiteSettings(locales: [$locale, en], stage: DRAFT, first: 1) {
    id
    siteName
    brandColor
    contactEmail
    footerText {
      text
    }
    logo {
      id
      url
      width
      height
    }
    socialLinks {
      id
      platform
      url
      handle
    }
    footerNavigation {
      id
      items {
        id
        label
        linkType
        externalUrl
        pageLink {
          id
          slug
        }
      }
    }
  }
}
